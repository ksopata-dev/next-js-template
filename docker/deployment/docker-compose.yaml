version: "3.0"

name: next-js-template-deployment
services:
  ansible:
    build: ./ansible
    container_name: ansible
    command: /bin/bash
    tty: true
    environment:
      - ANSIBLE_HOST_KEY_CHECKING=False
    ports:
      - "2223:22"

  nodejs:
    build: ./nodejs
    container_name: nodejs
    ports:
      - "2222:22"
